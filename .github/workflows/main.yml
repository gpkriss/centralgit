name: Decode Base64 Secret

on:
  push

env:
  GITHUB_PAT: ${{ secrets.GITHUB_TOKEN3 }}

jobs:
  decode-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Decode and print GH_TOKEN
        shell: bash
        run: |
          echo "Decoding GH_TOKEN..."
          echo "${{ secrets.GH_TOKEN }}" | base64 --decode || echo "❌ Invalid base64"
